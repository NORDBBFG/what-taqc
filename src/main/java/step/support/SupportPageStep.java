package step.support;

import org.openqa.selenium.WebDriver;
import step.BaseStep;

public class SupportPageStep extends BaseStep {

    public SupportPageStep(WebDriver driver) {
//        supportPage = new SupportPage(driver);
    }
}
